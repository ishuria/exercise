//
int* GetNext(char* p,int size){
    int* next = (int*)malloc(size*sizeof(int));
    next[0] = 0;
    for(int k=0,q=1;q<size;q++){
        while(k>0&&p[k]!=p[q]){
            k=next[k-1];
        }
        if(p[k]==p[q]){
            k++;
        }
        next[q]=k;
    }
    return next;
}